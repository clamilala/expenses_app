
<div class="container align-self-center col-md-7">

  <% if @jounal_entry.errors.present? %>
    <% @jounal_entry.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  <% end %>

  <div class="incomes_title">
    <div>収入の編集</div>
  </div>
  <div class="form-group incomes_form">
      <%= render 'form' %>
  </div>
</div>